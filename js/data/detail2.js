// 知识点详解加深包 —— 算法课程全新详解 + C#/Unity 重点知识点加厚
// 用 Object.assign 合并进 KP_DETAILS（同名 key 覆盖旧版，实现"加厚"）
window.KP_DETAILS = Object.assign(window.KP_DETAILS || {}, {
  // ================= C# 重点加厚 =================
  cs1_0: {
    more: [
      "值类型（int/float/bool/char/结构体）直接存数据，赋值时复制一份；引用类型（string/类/数组）存内存地址，赋值共享。这解释了\"把对象传进方法改属性，原对象也变\"。",
      "float 精度约 7 位有效数字，double 约 15 位。游戏开发的位置/速度用 float 足够且与 GPU 一致；金额等精确计算才用 decimal。",
      "int 与 float 混算是大坑：5 / 2 = 2（整数除法丢小数），5f / 2 = 2.5。血量百分比算错九成是这个。",
      "string 不可变：每次\"修改\"都创建新字符串。高频拼接必须 StringBuilder，否则 GC 疯狂报警。",
      "字面量后缀口诀：f→float、m→decimal、d→double、L→long；没有后缀的小数默认 double。"
    ],
    pit: "float pi = 3.14; 编译报错——double 字面量不能隐式转 float，必须 3.14f。新手最常见的第一个编译错误。",
    tip: "Unity 中速度、距离、百分比一律 float + f 后缀；血量等显示值转 int 再拼接文本。"
  },
  cs2_0: {
    more: [
      "x++ 与 ++x：前者先返回原值再自增，后者先自增再返回。单独一行等价，混进表达式天差地别——团队规范常禁止在复杂表达式里自增。",
      "取模 % 的妙用：x % 2 判奇偶、i % n 循环轮换（帧动画帧号）、(i+1) % n 环形前进。",
      "复合赋值 += 是\"读改写\"的缩写；对事件而言 +=/-= 是订阅语法，两个语境别混。"
    ],
    pit: "int 相除丢小数：5 / 2 = 2。想得 2.5 至少一边是浮点：5f / 2 或 5 / 2.0。",
    tip: "分数、连击、计时全部用 += 累加，配合取模做周期性触发（每 10 次结算一次）。"
  },
  cs3_0: {
    more: [
      "if 条件必须严格是 bool——C# 不允许 if (hp) 这种写法，强制你表达 hp > 0 还是 hp == 0，消灭歧义。",
      "else-if 链的顺序即优先级：从最特殊到最一般排列，否则被宽条件截胡。",
      "switch 的 case 在 C# 里不允许贯穿（除空分支），每个分支必须 break/return——比 C/C++ 安全。"
    ],
    pit: "在条件后多写一个分号 if (x > 0); 会让语句体变空，大括号里的代码永远执行。",
    tip: "状态超过 3 种就用枚举 + switch（或 switch 表达式），比 if 链可读性强一个量级。"
  },
  cs3_1: {
    more: [
      "for 与 while 能力等价，for 把\"初始/条件/步进\"收在一行适合计数；foreach 底层走迭代器，遍历中最干净。",
      "foreach 中给\"循环变量\"赋值是编译错误；但修改数组元素 arr[i] 合法——遍历 List 时 Remove 则直接抛异常。",
      "break 跳出本层、continue 跳过本层剩余部分；嵌套循环里 continue 只影响内层。"
    ],
    pit: "while(true) 忘写退出条件 = 编辑器假死。写 while(true) 时先确认 break 条件必然可达。",
    tip: "需要边遍历边删除时用\"倒序 for\"：for (int i = list.Count - 1; i >= 0; i--)。"
  },
  cs4_1: {
    more: [
      "List<T> 内部也是数组，容量不足时自动翻倍扩容（复制旧元素）。预估数量用 new List<T>(1000) 预分配，避免反复扩容的 GC 压力。",
      "Insert(下标, 值) 是\"插入挤位\"：后面的元素整体后移；想覆盖某位置用 list[下标] = 值（长度不变）。两者别混。",
      "Remove(值) 按值删、RemoveAt(下标) 按下标删——名字只差两个字母，行为天差地别；Remove 不存在的值静默不动。",
      "常用方法速记：Add、Remove、RemoveAt、Insert、Contains、IndexOf、Sort、Clear；Find/FindAll 接收谓词。"
    ],
    pit: "foreach 中 Remove 直接抛 InvalidOperationException；倒序 for 删除是标准解法。Count 与 Length 也别混写。",
    tip: "背包、波次、冷却列表用 List<T>；高频删除场景考虑倒序遍历或 HashSet。"
  },
  cs7_0: {
    more: [
      "继承表达 is-a：Dragon is a Monster。构造顺序先基类后派生——: base(...) 给基类构造传参。",
      "sealed 禁止继承；protected 让成员\"家族可见\"；override 重写、new 隐藏（隐藏不参与多态，是常见坑）。",
      "Unity 一切脚本皆 MonoBehaviour 子类：引擎持有基类引用统一调度，你的 Update 就是被多态调用的。"
    ],
    pit: "为\"复用代码\"强行继承（Player 继承 Enemy）是设计坏味道——组合（持有对象）通常更灵活。",
    tip: "继承层次控制在 2~3 层内：敌人→远程敌人→狙击手，自然分层最健康。"
  },
  cs9_0: {
    more: [
      "属性是\"穿马甲的方法\"：get/set 里加校验、通知、懒加载；调用方感觉像字段，类内保持控制。",
      "表达式体只读属性 public bool IsDead => hp <= 0; 每次现算，永远与 hp 同步——手动维护 isDead 布尔是经典 bug 来源。",
      "自动属性 { get; set; } = 10 支持初始化；private set 是\"对外只读、对内可控\"的标配。"
    ],
    pit: "在 get 里做重活（遍历/查找），调用方以为是字段访问造成性能陷阱——重计算用 GetXxx() 方法名提示。",
    tip: "对外状态用属性 + setter 通知事件（Hp 变化刷 UI），内部字段保持 private。"
  },
  cs11_0: {
    more: [
      "委托三步：声明类型 delegate void H(int)、实例化 H h = 方法/Lambda、调用 h?.Invoke(5)。",
      "多播委托按订阅顺序依次调用，+= 加、-= 删；调用列表可用 GetInvocationList 查看。",
      "内置三件套：Action（无返回）、Func（有返回，末位类型参数是返回值）、Predicate（返回 bool）——90% 场景无需自定义。"
    ],
    pit: "Lambda 无法退订（每次都是新实例）：需要退订就保存委托实例或用具名方法。",
    tip: "\"打完枪后做什么\"设计成 Action 参数：Fire(onHit, onMiss)——回调让系统灵活组合。"
  },
  cs11_1: {
    more: [
      "事件三角色：发布者声明 event Action<int> OnDied、订阅者 += 回调、触发 OnDied?.Invoke()。",
      "订阅是强引用：不退订就泄漏。标准姿势 OnEnable += / OnDisable -= 成对出现。",
      "静态事件全局可达最危险：谁都能订阅也谁都不退订——尽量用实例事件或集中管理。"
    ],
    pit: "在 Lambda 订阅里用了 this 却没退订，对象销毁后事件再触发就是 MissingReferenceException。",
    tip: "事件命名用过去时：OnDied、OnScoreChanged——订阅方一看就知道\"何时被调用\"。"
  },
  cs12_0: {
    more: [
      "LINQ 链式读法：Where 筛选 → Select 投影 → OrderBy 排序 → First/ToList 收尾。每步都返回可继续操作的序列。",
      "延迟执行：定义查询不跑，枚举才跑。同一查询枚举两次就执行两遍——需要时 ToList 缓存。",
      "性能红线：LINQ 的委托与分配开销在 Update 热路径会产生 GC 尖峰。冷路径放心用，热路径用 for。"
    ],
    pit: "First() 找不到直接抛异常——拿不准就 FirstOrDefault() 后判空。",
    tip: "常用速记：Any 存在判断、Count 统计、OrderByDescending 降序、Take/Skip 分页。"
  },
  cs12_1: {
    more: [
      "async/await 本质是状态机：await 挂起方法并记录后续，任务完成后恢复——不创建新线程。",
      "Unity 主线程同步上下文：await 后的代码默认回主线程，可以安全操作 GameObject。",
      "async void 无法等待且异常难捕捉——除事件处理器外一律返回 Task/Task<T>。"
    ],
    pit: "忘记 await 的 Task 会\"开跑但不管\"，异常被吞、时序错乱——编译器会给警告，别忽略。",
    tip: "多个请求并行：await Task.WhenAll(t1, t2) 后统一取结果，比串行 await 快数倍。"
  },
  u2_0: {
    more: [
      "MonoBehaviour 是引擎与你的契约：生命周期被引擎多态调用，transform/gameObject 等成员是访问引擎的快捷入口。",
      "一个物体可挂多个不同脚本组件——职责拆分（移动/战斗/背包各一个）比\"万能大脚本\"好维护得多。",
      "普通 C# 类（数据、工具、状态机逻辑）不继承 MonoBehaviour：可以 new、性能更好、不依赖场景。"
    ],
    pit: "文件名与类名不一致时组件挂不上（Missing Mono Script）——Unity 按文件名反序列化。",
    tip: "脚本顶部用 [Header] 分组参数、[Tooltip] 写说明，Inspector 就是自带文档的配置面板。"
  },
  u3_0: {
    more: [
      "Awake（引用缓存，先于所有 Start）→ OnEnable → Start（跨对象初始化）→ Update/LateUpdate → OnDestroy。",
      "FixedUpdate 与渲染帧率解耦：一帧内可能 0~N 次，物理代码专属；输入检测放 Update（物理帧可能漏检输入）。",
      "OnEnable/OnDisable 由激活状态成对触发——事件订阅/退订的标准位置。"
    ],
    pit: "Awake 里访问别的物体（可能还没 Awake）会空引用；跨对象初始化放 Start。",
    tip: "口诀：自己的事 Awake，别人的事 Start，每帧 Update，跟在后面 LateUpdate，物理 FixedUpdate。"
  },
  u7_0: {
    more: [
      "Rigidbody 属性速览：mass 质量、drag 阻力、useGravity 重力、isKinematic 运动学（不受力但触发事件）。",
      "移动方式选型：AddForce 推力（加速感）、velocity 直设（即时）、MovePosition（Kinematic 平滑移动）。",
      "碰撞体选型：Box/Sphere/Capsule 基础体性能好，MeshCollider 贵且需 Convex；角色首选 Capsule。"
    ],
    pit: "两个都没有 Rigidbody 的 Collider 相撞不会触发物理回调——至少一方要有刚体。",
    tip: "物理代码一律 FixedUpdate；input 检测放 Update 存成字段再在物理帧使用。"
  },
  u7_1: {
    more: [
      "触发器事件三连：Enter 进入、Stay 每物理帧、Exit 离开——拾取用 Enter、持续伤害用 Stay+计时。",
      "Layer Collision Matrix（Project Settings→Physics）可精确控制哪些层互交互，性能与逻辑双收。",
      "OnCollisionEnter 的 Collision 参数含 contacts（接触点）与 relativeVelocity（相对速度）——撞击伤害/特效的依据。"
    ],
    pit: "OnTriggerStay 是每物理帧调用，直接在里面加分会让分数暴涨——配计时器或改用 Enter+Exit 记录状态。",
    tip: "地面判断用 collision.contacts[0].normal.y > 0.5：只响应\"从上方踩到\"的碰撞。"
  },
  u9_0: {
    more: [
      "yield 家族：null（下一帧）、WaitForSeconds（缩放时间）、WaitForSecondsRealtime（真实时间）、WaitUntil（条件）、嵌套协程。",
      "协程依附 MonoBehaviour：物体禁用/销毁自动停止——Boss 死亡时它的连招协程自然终止。",
      "经典应用：技能前摇后摇、开场动画序列、波次刷怪、渐隐渐显（循环改 alpha + yield null）。"
    ],
    pit: "StartCoroutine 用字符串重载有反射开销且不支持重载参数——一律用方法调用版。",
    tip: "把流程写成协程最符合直觉：跳过大量状态机样板代码，剧情脚本系统就是它的豪华版。"
  },
  u10_0: {
    more: [
      "Canvas 渲染模式：Screen Space-Overlay（覆盖全屏，最常用）、Screen Space-Camera（受相机影响）、World Space（世界内 3D UI）。",
      "Canvas Scaler + Reference Resolution 一套 UI 适配全机型；先定分辨率再摆 UI，后期改锚点等于重摆。",
      "动静分离：血条、飘字等高频变化的 UI 拆到子 Canvas，避免整块 Canvas 重建造成卡顿。"
    ],
    pit: "所有 UI 挤一个 Canvas，任何元素变化触发整块 Rebuild——UI 卡顿的头号来源。",
    tip: "锚点九宫格想清楚\"相对谁定位\"：四角拉伸、边中跟随、中心居中，各得其所。"
  },
  // ================= 算法全新详解 =================
  a1_0: {
    more: [
      "大 O 忽略常数与低阶项：O(2n) 记作 O(n)、O(n²+n) 记作 O(n²)。它描述\"增长的形状\"而非精确耗时。",
      "常见级别尺子：O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ)。看到嵌套循环想 n²、看到减半想 log n。",
      "哈希查询 O(1) 是\"平均\"，最坏冲突退化为 O(n)——面试说平均/最坏都加分。"
    ],
    pit: "两个都是 O(n) 的算法无法只凭大 O 比快慢——常数因子要实测。但 O(n) 对 O(n²) 在大 n 下必胜。",
    tip: "写代码前先估算：n=10⁶ 时 O(n²) 是 10¹² 次操作，必然超时——直接逼自己想 O(n log n) 以内方案。"
  },
  a1_1: {
    more: [
      "二分的本质：每次比较排除一半搜索空间。log₂(10⁶)≈20，百万数据 20 次命中。",
      "List 扩容的\"均摊 O(1)\"：翻倍扩容虽然单次 O(n)，但 n 次 Add 总代价 O(n)，平摊每次 O(1)。",
      "递归 T(n)=T(n/2)+O(1) 解出 O(log n)——减半型递归都是对数级。"
    ],
    pit: "把\"平均 O(1)\"当成\"每次 O(1)\"——哈希最坏 O(n)，对实时性苛刻的场景（游戏主线程）要留意。",
    tip: "估算复杂度时用具体数字代入：n=10⁶ 时 O(n log n)≈2×10⁷ 次操作，现代 CPU 毫秒级完成。"
  },
  a2_0: {
    more: [
      "对撞指针：首尾向中间走，依赖\"单调性\"决定移动方向（回文比较、有序两数之和）。",
      "快慢指针：同向不同速——找链表中点（快2慢1）、判环（Floyd）、原地去重（读写指针）。",
      "同向读写指针：快读慢写，原地删除/移动/压缩数组——O(n) 时间 O(1) 空间的代表。"
    ],
    pit: "对撞指针写成 left <= right 在\"两数之和\"场景会重复比较甚至漏解——回文场景 <= 才对，看清题目语义。",
    tip: "指针移动前自问：这一步会不会漏掉可能的答案？用 3~5 个元素的小例子手推一遍。"
  },
  a2_1: {
    more: [
      "移动零：读写指针把非零前移，尾部补 0——一遍 O(n) 完成，保持相对顺序。",
      "有序数组平方排序：负数平方可能最大，最大值在两端——双指针从两端向中间填充结果。",
      "合并有序数组：从后往前填（比较两数组末尾大者），避免覆盖与额外空间。"
    ],
    pit: "双指针移动规则不严谨是头号 bug 来源——写之前用 3~5 个元素的小例子手推一遍指针轨迹。",
    tip: "三数之和 = 排序 + 固定一个数 + 双指针。排序 O(n log n) + O(n²) 内层，比暴力 O(n³) 快。"
  },
  a3_0: {
    more: [
      "哈希冲突用链地址法（桶挂链表）解决；冲突太多查询退化 O(n)——负载因子过高时自动扩容重建。",
      "键必须不可变且正确实现 GetHashCode/Equals——键被修改后哈希变化，永远查不回来。",
      "ContainsKey O(1)、ContainsValue O(n)——哈希只优化键。常查的维度才当键。"
    ],
    pit: "直接取不存在的键 d[key] 抛 KeyNotFoundException——TryGetValue 一次完成查找+取值。",
    tip: "词频统计、去重、两数之和——哈希三板斧覆盖大量面试题与游戏逻辑（背包、标记已读）。"
  },
  a3_1: {
    more: [
      "三个集合操作都修改\"调用者\"：IntersectWith 交集、UnionWith 并集、ExceptWith 差集。",
      "HashSet 不保序：需要顺序时先排序结果，或改用 SortedSet。",
      "集合运算都是 O(n/m) 线性级——比双重循环的 O(n×m) 强得多。"
    ],
    pit: "HashSet 的元素顺序遍历结果与插入顺序无关——依赖顺序的需求选 List + Contains 检查。",
    tip: "两个列表求\"新增/删除\"差异：ExceptWith 两次（新集合减旧、旧减新）即得增删清单。"
  },
  a4_0: {
    more: [
      "对撞指针：l 与 r 相向，比较后移动一侧。终止条件 i<j（相遇即完成）。",
      "移动零：读写指针——非零前移、末尾补零，保持相对顺序 O(n)。",
      "有序数组平方排序：最大绝对值在两端，双指针从两端向中间填充结果。"
    ],
    pit: "对撞指针找到答案后忘了 return，继续收缩反而漏解/死循环——命中即返回或记录后跳出。",
    tip: "反转数组 = 对撞指针交换 n/2 次——原地反转是一切双指针题的入门。"
  },
  a4_1: {
    more: [
      "\"原地\"算法 = 额外空间 O(1)：反转、去重、移除元素都要求原地完成。",
      "部分区间反转：初始位置换成 l 与 r 即可——整体反转是它的特例。",
      "回文链表 = 快慢找中点 + 反转后半 + 比较（可选还原）——链表三大技巧联合作战。"
    ],
    pit: "原地操作修改了输入数组，调用方若还依赖原数据会出 bug——函数注释里写明\"会修改原数组\"。",
    tip: "面试先说时空复杂度：\"O(n) 时间、O(1) 空间\"一句话展现思路清晰度。"
  },
  a5_0: {
    more: [
      "栈 LIFO：Push/Pop/Peek。撤销、浏览器后退、括号匹配、表达式求值全是栈。",
      "有效括号两条件：扫描中深度不为负 + 结束时深度为 0。数量相等不等于嵌套正确。",
      "Peek 与 Pop 的区别：看不动 vs 看并弹出；空栈调用都抛异常。"
    ],
    pit: "只统计左右括号数量相等就判定有效是错的——\"(}\"数量匹配但嵌套错误。",
    tip: "Unity 里的典型应用：操作撤销栈（配对象池回收 UI 记录）、技能连招的输入缓冲。"
  },
  a5_1: {
    more: [
      "队列 FIFO：Enqueue/Dequeue/Peek。任务调度、打印队列、BFS 层级遍历。",
      "循环队列用 % 容量回绕指针，复用空间；判满需要留空位或维护计数。",
      "两个栈可以实现队列：入栈倒进出栈，均摊 O(1)。"
    ],
    pit: "队列空时 Dequeue/Peek 抛异常——Unity 消息队列处理前先判 Count。",
    tip: "口诀：队广栈深（BFS 队列、DFS 栈）；\"先来先服务\"用队列，\"最近的最优先\"用栈。"
  },
  a6_0: {
    more: [
      "虚拟头节点 dummy 统一头部操作：删除头/插入头不再特判，返回 dummy.next。",
      "快慢指针找中点：慢 1 快 2；偶数长度停在第二个中点（约定看题目）。",
      "反转后半段 + 双向比较 = O(1) 空间判回文链表——三大技巧联合作战。"
    ],
    pit: "C# 没有指针回收，删除节点靠\"绕过\"（prev.next = node.next）——被绕过的节点交给 GC。",
    tip: "链表题先画图再写代码：画出指针如何重接，代码只是图的翻译。"
  },
  a6_1: {
    more: [
      "数组 vs 链表：读 O(1)/O(n)，插入删除 O(n)/O(1)（已知位置）——按读写比例选型。",
      "双向链表 prev 指针让\"删已知节点\"O(1)——LRU 缓存 = 哈希表 + 双向链表。",
      "数组模拟链表（静态链表）：下标当指针，内存连续无动态分配——竞赛/嵌入式常用。"
    ],
    pit: "Unity 的 List<T> 是数组封装：中段 Remove 会移动后续元素——高频中段删除换 HashSet/LinkedList。",
    tip: "链表操作前画 3 个节点的小图，指针重接一目了然，纯脑补必错。"
  },
  a7_0: {
    more: [
      "递归三要素：基线条件（何时停）、递归步骤（如何缩小）、合并结果（如何拼答案）。",
      "调用栈每层存参数与局部变量——深度大就 StackOverflowException，且无法被 try-catch 捕获。",
      "递归树是调试利器：节点重复多 → 记忆化；深度太深 → 改迭代。"
    ],
    pit: "递归算阶乘到 13! 就溢出 int——大数值换 long，或中途取模。",
    tip: "把\"打印语句在递归前后\"的顺序差异吃透：先打印正序、后打印倒序。"
  },
  a7_1: {
    more: [
      "分治三步：分（拆）、治（递归解）、合（合并）。归并、快排、二叉树问题都是分治。",
      "剪枝：在搜索树中提前排除不可能分支，让指数级搜索显著变小。",
      "递归转迭代：用显式栈自己管理\"栈帧\"——深度可控、逻辑等价。"
    ],
    pit: "依赖尾递归优化的写法在 C# 不可靠（JIT 不保证优化）——深递归请显式化。",
    tip: "写递归先写基线再写递归步骤，顺序反了容易漏终止条件。"
  },
  a8_0: {
    more: [
      "稳定排序保相对顺序：多关键字排序（先分数后名字）依赖它。归并/插入/计数稳定，快排/选择/堆不稳。",
      "近乎有序的数据：插入排序 O(n) 表现惊艳——快排对小块递归到插入排序正是内省排序的混合思想。",
      "C# 的 Array.Sort 是内省排序：平均与最坏都是 O(n log n)。"
    ],
    pit: "对引用类型排序要提供比较器：Sort((a,b) => a.score.CompareTo(b.score))——b 在前是降序。",
    tip: "只排序一段用 Array.Sort(arr, index, count) 重载——排行榜分页排序省时间。"
  },
  a8_1: {
    more: [
      "计数排序：值域小的整数 O(n+k) 线性排序——用空间换时间的极端案例。",
      "各排序速记：冒泡/选择教学用、插入近乎有序、归并稳定、快排通用、计数整数小范围。",
      "Array.Reverse(arr, start, count) 部分反转；Sort 同样支持区间重载。"
    ],
    pit: "计数排序对大范围/浮点无效——桶装不下就没有意义，选择排序算法先看数据特征。",
    tip: "游戏排行榜一般直接 Sort + Lambda；性能敏感的高频排序才需要专门数据结构（堆/跳表）。"
  },
  a9_0: {
    more: [
      "DP 三问：状态是什么（dp[i] 含义）、转移方程（怎么推）、边界（初值）。",
      "打家劫舍方程 dp[i] = max(dp[i-1], dp[i-2] + v[i])：\"偷或不偷\"两种决策取大。",
      "记忆化（自顶向下递归+缓存）与制表（自底向上循环）殊途同归。"
    ],
    pit: "边界初始化错一个，后面全错——dp[0]/dp[1] 用最小例子验证后再写循环。",
    tip: "DP 题先暴力递归 → 发现重复 → 加缓存 → 改循环，四步演进理解最深。"
  },
  a9_1: {
    more: [
      "空间优化：dp[i] 只依赖前 1~2 项时用滚动变量，O(n) → O(1)。",
      "无后效性：当前状态定死后续就不受历史路径影响——不满足时给状态加维度。",
      "计数 DP 结果巨大时按题意对 10⁹+7 取模——每步取模不影响加减乘（除法除外）。"
    ],
    pit: "贪心不是万能的：{1,3,4} 凑 6，贪心给 4+1+1 三枚，DP 才能找到 3+3 两枚。",
    tip: "识别 DP 的信号词：\"最少/最多/多少种方法\"。看到它们先想状态与转移。"
  },
  a10_0: {
    more: [
      "前缀和：P[i] = a[0..i] 之和，区间和 = P[r] - P[l-1]。O(n) 预处理换 O(1) 查询。",
      "二维前缀和用容斥：减两侧、加回角。图像积分图（Integral Image）就是它。",
      "差分数组：区间整体加减 O(1)，最后一次前缀和还原——前缀和的逆运算。"
    ],
    pit: "l=0 时 P[l-1] 越界——用\"下标从 1 开始\"的前缀和数组（P[0]=0）统一处理。",
    tip: "积分图让\"任意矩形区域亮度求和\"O(1) 完成——全屏泛光、区域判定都在用。"
  },
  a10_1: {
    more: [
      "滑动窗口：右扩张、左收缩，每元素最多进出各一次 → O(n)。适合\"连续子数组/子串\"问题。",
      "二分答案：答案空间单调时对\"答案\"二分+贪心验证——不只查数组。",
      "哈希 + 前缀和：P[r]-K 在哈希里找首次出现 → \"和为 K 的最长子数组\"O(n)。"
    ],
    pit: "贪心策略先证\"局部最优可推出全局最优\"再用——{1,3,4} 凑 6 的反例说明贪心会错。",
    tip: "工程估算套路：各阶段复杂度相加取主导项（筛选 O(n) + 排序 O(m log m)）。"
  }
});
